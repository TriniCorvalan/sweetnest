<div id="successModal" class="fixed inset-0 bg-black/80 backdrop-blur-sm hidden z-50 flex items-center justify-center p-4">
  <div class="glass-effect rounded-3xl p-12 max-w-md w-full shadow-2xl text-center">
    <div class="text-8xl mb-6">🎉</div>
    <h2 class="text-3xl font-bold text-white mb-4">Pedido confirmado</h2>
    <p class="text-white/90 mb-6 text-lg">Tu SweetNest se esta preparando con cuidado. Llega en 3 a 5 dias.</p>
    <p class="text-yellow-300 font-bold text-xl mb-4">Pedido #<span id="orderNumber"></span></p>
    <div class="text-left bg-white/10 border border-white/20 rounded-2xl p-4 mb-8">
      <p class="text-white/90 text-sm mb-2">Te enviaremos por correo los datos para realizar la transferencia bancaria.</p>
      <p class="text-white/90 text-sm">
        La orden tiene vigencia de <strong>24 horas</strong>. Si no se registra la transferencia en ese plazo, se cancelara automaticamente.
      </p>
    </div>
    <button id="completeOrder" class="w-full bg-gradient-to-r from-emerald-400 to-teal-500 text-white py-4 px-8 rounded-2xl font-bold text-lg shadow-glow hover:shadow-2xl btn-glow">Confirmado</button>
  </div>
</div>

